using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.InputSystem;

public class CameraShoulder : MonoBehaviour
{
   /* [SerializeField] private float _speedLookAT = 10;

    //Fait tourner la camera vers la cible en suivant l'index dans le tableau d'ennemis recuperer dans PlayerController
    public void LookEnemy(bool onShoulder, bool canLook, Transform enemy)
    {
        if(canLook)
        {
            Quaternion targetRotation = Quaternion.LookRotation(enemy.transform.position - transform.position);
            transform.rotation = Quaternion.Slerp(transform.rotation, targetRotation, _speedLookAT);
        }
    }*/

}
